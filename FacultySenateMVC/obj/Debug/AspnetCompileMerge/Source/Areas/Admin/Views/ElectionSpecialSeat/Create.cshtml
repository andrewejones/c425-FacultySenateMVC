@model FacultySenateMVC.Models.ElectionSpecialSeat

@{
    ViewBag.Title = "Create Special Election Seat";
}

<h2>Create Special Election Seat</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Special Election Seat</legend>

        <div class="editor-label">
            Election Id
        </div>
        <div class="editor-field">
            @Html.DropDownList("ElectionId", String.Empty)
            @Html.ValidationMessageFor(model => model.ElectionId)
        </div>

        <div class="editor-label">
            Committee Seat Id
        </div>
        <div class="editor-field">
            @Html.DropDownList("CommitteeSeatId", String.Empty)
            @Html.ValidationMessageFor(model => model.CommitteeSeatId)
        </div>

        <div class="editor-label">
            Term Length
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TermLength)
            @Html.ValidationMessageFor(model => model.TermLength)
        </div>

        <div class="editor-label">
            Term End Date
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TermEndDate)
            @Html.ValidationMessageFor(model => model.TermEndDate)
        </div>

    </fieldset>

    <div>
        <input type="submit" value="Create" /> | 
        @Html.ActionLink("Back to List", "Index")
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
